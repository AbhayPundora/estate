apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: estate-catalog
  title: Estate Platform Catalog
  description: Root catalog that references all Estate platform entities.
spec:
  targets:
    - ./client/catalog-info.yaml
    - ./api/catalog-info.yaml
    - ./openapi.yaml
    - ./system.yaml
    - ./group.yaml
